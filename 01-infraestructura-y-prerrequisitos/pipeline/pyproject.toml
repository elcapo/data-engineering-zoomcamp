[project]
name = "zoomcamp-pipeline"
version = "0.1.0"
description = "Pipeline ETL para procesamiento de datos"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pgcli>=4.4.0",
]
