[project]
name = "climate-flink"
version = "0.1.0"
description = "Flink SQL templates and runner for the climate-and-development pipeline."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["jobs"]
